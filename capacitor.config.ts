import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
	appId: 'com.visitedvountries.app',
	appName: 'VisitedCountries',
	webDir: 'dist',
	bundledWebRuntime: false,
}

export default config
